android {
    ...
    applicationVariants.all { variant ->
        variant.outputs.all { output ->
            def projectN = "Istanbul-Taksimetre"
            outputFileName = "${projectN}-${variant.name}.apk"
        }
    }
}
